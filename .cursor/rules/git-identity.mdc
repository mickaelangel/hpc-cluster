---
description: Identité Git du dépôt uniquement — jamais cursoragent / Cursor Agent
alwaysApply: true
---

# Identité Git pour les commits

- Lors de tout `git commit`, utiliser **uniquement** l'identité du dépôt : `Mickael Angel` / `mickaelangelcv@gmail.com` (ou celle définie par `git config user.name` et `user.email` dans ce repo).
- **Interdit** : ne jamais utiliser ni apparaître comme auteur/committer sous les noms **cursoragent**, **cursorsagent**, **Cursor Agent** ou tout compte/bot Cursor. Le dépôt ne doit avoir qu’un seul contributeur humain (Mickael Angel) en plus des bots explicites (ex. dependabot).
- Ne pas définir `GIT_AUTHOR_NAME`, `GIT_COMMITTER_NAME`, `GIT_AUTHOR_EMAIL`, `GIT_COMMITTER_EMAIL` vers un identifiant Cursor/cursoragent.
- Objectif : cursoragent ne doit **jamais** apparaître dans les contributeurs GitHub et ne doit **jamais** être notifié.
